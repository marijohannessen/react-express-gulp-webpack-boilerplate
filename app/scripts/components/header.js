const React = require('react');

// <Header />
const Header = React.createClass({
  render : function() {
    return (
      <header>
        <h1>React Boilerplate</h1>
        <h2>with Gulp, Express, Webpack</h2>
      </header>
    )
  }
});

export default Header;
